import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.jok.scan-products',
  appName: 'Scan Products',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
